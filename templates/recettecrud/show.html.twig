{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recette.name }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ recette.time }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recette.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ recette.price }}</td>
            </tr>
            <tr>
                <th>IsFavorite</th>
                <td>{{ recette.isFavorite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ recette.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recettecrud_index') }}">back to list</a>

    <a href="{{ path('app_recettecrud_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recettecrud/_delete_form.html.twig') }}
{% endblock %}
