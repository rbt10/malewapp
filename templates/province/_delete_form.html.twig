<form method="post" action="{{ path('app_province_delete', {'id': specialite_province.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialite_province.id) }}">
    <button class="btn">Delete</button>
</form>
