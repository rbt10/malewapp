{% extends 'base.html.twig' %}

{% block title %}SpecialiteProvince{% endblock %}

{% block body %}
    <h1>SpecialiteProvince</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite_province.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ specialite_province.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_province_index') }}">back to list</a>

    <a href="{{ path('app_province_edit', {'id': specialite_province.id}) }}">edit</a>

    {{ include('province/_delete_form.html.twig') }}
{% endblock %}
