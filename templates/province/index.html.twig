{% extends 'base.html.twig' %}

{% block title %}SpecialiteProvince index{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <h1 class="display-6">Recette de la province{{ province.libelle }}</h1>
            <!-- On affiche que les recette publique -->
            {% for recette in recettes %}
                <div class="col-md-4 col-lg-6 ">
                    {% include 'recette/single.html.twig' %}
                </div>
            {% endfor %}

        </div>
    </div>



{% endblock %}
